class Component {
    constructor(props) { // 获取到子类传过来的值
        this.props = props; 
    }
    setState() { // 更新状态 setState + domdiff
        console.log('asdadadaxdasf')
    }
}
export default Component;